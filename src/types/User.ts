export interface User {
    name: string;
    email: string;
    userKey: string;
    groupKey: string;
}

export interface RegisteredUser {
    name: string;
    email: string;
    userKey: string;

}